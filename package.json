{
  "name": "ez-ali-futuristic-portfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "predev": "node scripts/generate-projects-data.js",
    "dev": "vite --host 0.0.0.0",
    "prebuild": "node scripts/generate-projects-data.js",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "devDependencies": {
    "sharp": "^0.35.3",
    "vite": "^7.0.0"
  },
  "dependencies": {
    "gsap": "^3.15.0"
  }
}
